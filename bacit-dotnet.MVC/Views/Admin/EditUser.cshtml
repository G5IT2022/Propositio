<!--View for å redigere ansatt-->

@using bacit_dotnet.MVC.Entities
@using bacit_dotnet.MVC.Models.AdminViewModels
@using bacit_dotnet.MVC.Models
@using Microsoft.AspNetCore;
@using bacit_dotnet.MVC.Models.AdminViewModels.TeamModels;
@model AdminEditUserModel;

@{
    ViewBag.Title = "Admin | Redigere ansatt";
}

<h2>Edit</h2>

<form asp-action="UpdateUser" asp-controller="admin" method="post">
        @Html.AntiForgeryToken()
        <div class="form-horizontal">
            <h4>Redigere Ansatt</h4>

            <hr />
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.emp_id)

            <!--fornavn-->
            <div class="form-group">
                @Html.LabelFor(model => model.first_name, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.first_name, new { htmlAttributes = new { @class = "form-control", @placeholder=@Model.user.name} })
                    @Html.ValidationMessageFor(model => model.first_name, "", new { @class = "text-danger" })
                </div>
            </div>
            <br />
            <!--Etternavn-->
            <div class="form-group">
                @Html.LabelFor(model => model.last_name, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.last_name, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.last_name, "", new { @class = "text-danger" })
                </div>
            </div>
            <br />
            <!--Passord-->
            <div class="form-group">
                @Html.LabelFor(model => model.password, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.password, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.password, "", new { @class = "text-danger" })
                </div>
            </div>
            <br />

            <!--Velg rolle-->
            <div class="form-group">
                @Html.LabelFor(model => model.possibleRoles, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="form-group mb-2">
                    @Html.HiddenFor(model => model.possibleRoles, new {@class="control-label"})
                    @Html.DropDownListFor(m => m.role_id, Model.possibleRoles, new { @class="form-control"})
                </div>
            </div>

            <!--Admin-->

            <div class="form-group">
                <div>
                    @Html.ActionLink("Tilbake til ansattlisten", "Index", new {})
                </div>
                <button type="submit" class="btn btn-primary">Lagre endringer</button>
            </div>
        </div>
</form>
